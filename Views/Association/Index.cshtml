@model IEnumerable<PEMO_DATA_BANKING.Models.Association>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Association_name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Association_name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Association_id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Association_id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Association_id })
        </td>
    </tr>
}

</table>
